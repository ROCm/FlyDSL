# The dependencies to run tests

# Include base requirements
-r requirements.txt

# PyTorch ROCm index (for ROCm-specific builds)
--extra-index-url https://download.pytorch.org/whl/rocm7.1

# Test-specific dependencies
pytest
pandas
torch

