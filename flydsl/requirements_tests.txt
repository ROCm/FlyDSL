# the dependencies to run tests

# Include base requirements
-r requirements.txt

# Test-specific dependencies
pytest
pandas

# Note: torch is installed separately by install_test_requirements.sh
# The script checks if torch is already installed and only installs from
# ROCm index (https://download.pytorch.org/whl/rocm7.1) if needed